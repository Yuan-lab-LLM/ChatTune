# 用户指南

本文说明普通用户在 Studio 中的常用操作。

## 登录

浏览器打开 `STUDIO_PUBLIC_URL`，使用管理员创建的账号登录。

登录会话默认有效 7 天；连续登录失败默认达到 5 次会锁定约 15 分钟。锁定状态当前保存在 Studio 进程内存中，重启进程后会清空。

用户可以从账户菜单修改自己的密码。修改成功后，已有会话会失效，需要重新登录。

## 可用的自然语言命令

用户登录 Studio 后，可直接在对话框中输入自然语言任务。

注意：运行页面右上角的“清空上下文”会清除当前页面对话上下文，并开启新的上下文。系统会随新上下文更新当前会话用户名；因此，“当前用户”“我的”等概念只在同一个上下文中保持一致。清空上下文后，如需继续操作原上下文中的任务或实例，请在命令中明确提供任务 ID、实例 ID、工作流 ID 或容器名称。

## 一键工作流

一键工作流用于把一次完整的模型迭代串联起来。用户选择训练数据集和评测集后，系统会依次推进以下阶段：

1. 模型训练：根据数据位置自动执行 LoRA 批量训练/增强训练。
2. 医疗评估：对训练产出的模型执行单模型评估。
3. 模型发布：将训练结果复制到配置的发布目录并形成可部署版本。
4. 推理部署：更新推理配置、重启推理服务并确认服务状态。
5. 基准评测：使用选定的评测集运行推理 Benchmark。

工作流启动后，页面状态栏会持续显示当前阶段及训练、评估、发布、部署和基准评测的进度。工作流状态会持久化；任务失败时可根据页面显示的原因处理后继续执行。

### 页面操作步骤

1. 进入项目的运行页面，在右上角区域选择“一键工作流”按钮。
2. 在右侧“一键工作流”面板中选择训练数据集。训练数据集为必选项；如列表未更新，可点击“刷新数据集”。
3. 选择评测集。评测集为可选项；未选择时默认使用 `2024.json`。
4. 检查面板中的命令预览，然后点击“生成命令”。该操作会把命令放入聊天输入框。
5. 确认生成的命令和数据集无误后，点击聊天框右下角的发送按钮，正式执行一键工作流。
6. 工作流启动后，通过页面状态栏查看各阶段进度。

### 工作流控制命令

| 用途 | 自然语言示例 |
| --- | --- |
| 查看状态 | `查看一键工作流状态` |
| 停止工作流 | `停止当前一键工作流` |
| 继续失败任务 | `继续上次失败的一键工作流` |
| 查看指定工作流 | `查看一键工作流状态 <工作流ID>` |
| 停止指定工作流 | `停止一键工作流 <工作流ID>` |

工作流 ID 形如 `wf-YYYYMMDDHHMMSS-xxxxxxxx`，启动后会显示在状态信息中。同时存在多个工作流时，应在控制命令中带上工作流 ID。

## 数据处理

| 用途 | 自然语言示例 |
| --- | --- |
| 数据预处理 | `执行数据预处理` |
| 数据高级筛选 | `执行数据高级筛选处理` |
| 数据高级筛选（指定阈值） | `执行数据高级筛选，数据清洗阈值=60` |

参数不足时，Agent 会继续询问。

数据预处理支持 `sft`、`dpo` 数据格式；针对医疗文本数据支持根据数据用途选择 `inspection`、`diagnosis` 或 `prescription`。

数据预处理默认直接使用最新日期的数据进行处理，若想使用较旧的数据集，可在左侧数据管理页签下选择想要的数据，并点击 `Preprocess with this` 按钮。

## 单机训练

| 用途 | 自然语言示例 |
| --- | --- |
| LoRA 批量训练 | `执行lora批量训练` |
| LoRA 批量训练自定义参数 | `执行lora批量训练，MBS=1，ACC=8，LR=1e-7` |
| 全参批量训练 | `执行全参批量训练` |
| DPO 增强训练 | `执行增强训练` |
| GRPO 训练 | `执行grpo训练` |

参数不足时，Agent 会继续询问。

当前版本的批量训练与增强训练默认支持 Qwen3 系列的训练和评估。若需使用其他模型，请在执行训练时，通过 TEM 或 template 参数显式指定模型模板，例如：执行lora批量训练，TEM=llama3 或 执行增强训练，template=deepseekr1。

LoRA 批量训练/全参批量训练默认直接使用/home/workspace/models/base下最新模型进行训练；若想使用其他模型，需显示指定，如`执行lora批量训练，模型在xxxx`

LoRA 批量训练/全参批量训练默认直接使用最新日期的数据进行训练；若想使用较旧的数据集，可在左侧数据管理页签下选择想要的数据，并点击 `Train with this` 按钮。

## 多机训练

多机训练相关能力计划在下一阶段支持。

## 医疗评估

| 用途 | 自然语言示例 |
| --- | --- |
| 单模型评估 | `执行单模型评估` |
| 双模型对比评估 | `执行双模型评估` |
| Checkpoint 评估 | `执行checkpoint评估` |

参数不足时，Agent 会继续询问。

Checkpoint 评估默认支持 Qwen3 系列，若评估其他模型的checkpoint，请在评估命令中通过 TEM 或 template 参数指定模板。

## 状态和停止任务

| 用途 | 自然语言示例 |
| --- | --- |
| 查询训练状态 | `查询当前训练状态` |
| 查询评估状态 | `查询当前评估状态` |
| 停止训练 | `停止当前训练任务` |
| 停止评估 | `停止当前模型评估任务` |
| 停止数据处理 | `停止当前数据处理任务` |

为避免停止错误任务，同时存在多个任务时应补充任务类型、PID、工作流 ID 或容器名称。启动任务后，建议保留系统返回的 PID、运行 ID 和容器信息，便于后续查询或停止。

## 推理服务、功能测试和基准测试

推理相关命令会转发给推理 Agent。执行前应确保 `INFERENCE_AGENT_URL` 指向推理 Agent controller 的 `/inference_agent` 接口，controller 和 worker 已启动，并且目标节点已经创建和配置推理 Docker。推理服务 YAML 和节点配置见 [部署指南](DEPLOYMENT_ZH.md)。

### 查看和修改推理配置

| 用途 | 自然语言示例 |
| --- | --- |
| 查看全部配置（多机部署时返回全部节点） | `查看推理配置文件` |
| 查看指定节点配置 | `查看节点main/node1的推理配置` |

通过修改配置页面的参数并保存从而修改推理配置（仅管理员）。

### 推理服务和节点运维

普通用户只能查看和操作自己有权限的推理服务实例；查看“所有”实例或停止其他用户实例需要管理员账号。停止类操作建议先使用预览命令，确认目标无误后再执行确认命令。

| 用途 | 自然语言示例 | 说明 |
| --- | --- | --- |
| 查看服务状态（多机部署时返回全部节点） | `查看推理状态` | 返回推理服务整体运行状态。 |
| 查看启动状态 | `查看推理服务启动状态` | 返回当前启动任务状态。 |
| 启动服务 | `启动推理服务` | 启动推理服务。 |
| 关闭服务 | `关闭推理服务` | 关闭推理服务。 |
| 查看日志 | `查看推理服务日志` | 查看服务日志。 |
| 指定节点操作 | `查看节点 <节点名称> 的推理服务状态`、`重启节点 <节点名称> 的推理服务` | 多机部署时用于指定节点。 |
| 查看当前用户推理服务实例 | `查看当前推理服务实例列表` | 返回当前用户的推理服务实例。 |
| 查看指定实例状态 | `查看推理服务实例 instance_id=20260731_161456_26261a30 的状态` | 普通用户只能查看自己的实例；管理员可查看指定实例。 |
| 预览停止实例 | `预览停止推理服务实例 instance_id=20260731_161456_26261a30` | 仅检查将停止的实例和阻塞任务，不执行停止。 |
| 确认停止实例 | `确认停止推理服务实例 instance_id=20260731_161456_26261a30` | 普通用户只能停止自己的实例；管理员可停止指定实例。 |
| 管理员查看所有实例 | `查看所有推理服务实例` | 管理员用于排查跨用户实例。 |

多机部署时建议在命令中明确节点名称，避免操作错误节点。

### 推理功能测试

普通用户只能查看和操作自己的功能测试任务；查看所有任务或停止其他用户任务需要管理员账号。停止类操作建议先使用预览命令。

| 用途 | 自然语言示例 | 说明 |
| --- | --- | --- |
| 查看测试脚本 | `查看可用的推理功能测试脚本` | 返回当前可用脚本。 |
| 运行测试 | `运行推理功能测试脚本 basicmedicalrecord.sh` | 使用真实脚本名运行测试。 |
| 查看测试状态 | `查看推理功能测试状态` | 查看当前测试状态。 |
| 预览停止功能测试 | `预览停止功能测试 test_run_id=20260728_155822_eef19ec3` | 仅检查目标测试状态，不终止进程。 |
| 确认停止功能测试 | `确认停止功能测试 test_run_id=20260728_155822_eef19ec3` | 普通用户只能停止自己的任务；管理员可停止指定任务。 |
| 管理员查看所有功能测试任务 | `查看所有功能测试任务` | 返回所有用户功能测试运行记录。 |

应先查看可用脚本，再使用返回的真实脚本名运行测试。

### 推理基准测试

普通用户只能查看和操作自己的 Benchmark 任务；查看所有任务或停止其他用户任务需要管理员账号。停止类操作建议先使用预览命令。

| 用途 | 自然语言示例 | 说明 |
| --- | --- | --- |
| 查看可用 Benchmark | `查看可用的推理基准测试` | 返回当前可用 Benchmark。 |
| 启动 Benchmark | `运行推理基准测试2024.json` | 启动指定 Benchmark。 |
| 查看状态 | `查看推理基准测试2024.json状态` | 查看任务状态。 |
| 查看进度 | `查看推理基准测试2024.json进度` | 查看执行进度。 |
| 查看结果 | `查看推理基准测试2024.json结果` | 查看结果信息。 |
| 停止 Benchmark | `停止推理基准测试2024.json` | 停止当前用户的 Benchmark 任务。 |
| 指定节点运行 | `在节点 <节点名称> 运行推理基准测试2024.json` | 多机部署时指定节点运行。 |
| 预览停止 Benchmark | `预览停止推理基准测试 job_id=1785225697_81e359` | 仅检查目标任务状态，不终止进程。 |
| 确认停止 Benchmark | `确认停止推理基准测试 job_id=1785225697_81e359` | 普通用户只能停止自己的任务；管理员可停止指定任务。 |
| 管理员查看所有 Benchmark 任务 | `查看所有 benchmark 任务` | 返回所有用户 Benchmark 任务。 |

系统支持医疗选择题数据集（如 `2021.json`、`2024.json`、`step1.json`、`step2.json`、`step3.json`）、MedBench 和通用 Benchmark。推理基准评测需要基于开发人员提供的 `tests` 文件夹，包含当前所有已支持的 benchmark。部署前请确认该目录已随 `medflow/` 一起挂载到推理容器中，并且 `service.yaml` 中的 Benchmark 数据目录指向容器内实际路径。实际可用项以“查看可用的推理基准测试”的返回结果为准。

### 管理员维护命令

以下命令用于清理本地残留资源，仅管理员账号可执行。清理类操作建议先使用预览命令，确认目标无误后再执行确认命令。

| 用途 | 自然语言示例 | 说明 |
| --- | --- | --- |
| 预览清理本地残留资源 | `预览清理本地残留资源` | 展示 stale 实例、残留进程或可清理记录，不执行清理。 |
| 确认执行本地残留资源清理 | `确认执行本地残留资源清理` | 按预览结果执行清理。 |
